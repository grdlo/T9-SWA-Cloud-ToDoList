import { TextField } from "@material-ui/core";

export default {
    modal: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center'
    },
    formRoot: {
        '& > *': {
            margin: 16
        }
    }
}